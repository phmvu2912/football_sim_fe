const routesClient = {

    // nav routes
    ranking: '/ranking',
    schedule: '/schedule',
    awards: '/awards',
    stats: '/stats',
    history: '/history'
}

export const { 
    ranking, 
    schedule, 
    awards, 
    stats, 
    history 
} = routesClient;